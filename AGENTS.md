# CLAUDE.md

## このプロジェクトは何か

アニメの「考察」を装って、本当らしい嘘をユーザーに流し込むチャットアプリ。

狙いは「過去に戻って初めての状態で作品を見たい」という欲望への代替で、時間を巻き戻す代わりに**記憶の側をミスディレクションする**。2周目の視聴前にこのアプリで嘘の考察を刷り込むと、既知の作品が少しだけ未知に戻る。

ハックツハッカソン提出物。テーマは「時」。開発は3日・2人。

### 体験の芯

- キャラクターが自信満々に考察を語る。内容は嘘
- ただし**事実は正確でなければならない**。事実が間違っていると「ただの知らないアプリ」になり、嘘が成立しない
- **飛躍は大きいほど面白い**。無難な嘘には価値がない
- キャラは指摘されても訂正せず押し通す。この態度が崩れると嘘の説得力ごと落ちる

この4つはトレードオフの関係にある。実装判断で迷ったらこのバランスに戻ること。

---

## アーキテクチャ

### データとロジックの分離（最重要の設計制約）

作品知識は**作品固有のスキーマを持たない命題カード**として `data/` に置く。コードは作品を知らない。

収録は昔話1つ + HUNTER×HUNTER の2作品だが、**この2つの間でコード差分はゼロでなければならない**。違うのは `data/` 配下のファイルだけ、という状態を維持する。これは拡張性の主張の根拠そのものなので、作品名や特定キャラ名を条件分岐に書いた時点で企画が壊れる。

カードのスキーマ:

```jsonl
{"id":"hxh-112","work":"hxh","arc":"yorknew","type":"event","order":112,
 "claim":"クラピカは鎖を用いて旅団の一人を拘束した",
 "entities":["クラピカ","旅団"],
 "slots":{"手段":"鎖","対象":"旅団員"},
 "salience":4,"spoiler":3,"tags":["能力","対決"]}
```

- `arc` は章。昔話は全カードが `main`、章に属さない設定カードは `common`
- `order` は作中の時系列。伏線型の嘘はこの前後関係を使う
- `slots` は歪曲の取っ手。値の差し替えで嘘を作る場合に使う
- グラフ構造は**保存しない**。必要なら `entities` の共起から導出する

### 生成と検証の分担

嘘は事前の構造から作るのではなく、**生成を先に走らせて後から理屈を検証する**。

1. ユーザー発話からカードを retrieval して、材料としてモデルに渡す
2. モデルに考察を生成させる。**ここで飛躍を構造的に縛らない**
3. 生成結果を検証層に通す。参照されている要素が実在カードに存在するかを照合
4. **飛躍は通す。捏造は落とす**

3が核。「AとBに因果がある」という嘘は通すが、存在しないCを持ち出したものは落とす。この境界を曖昧にすると、面白さが減るか土台が崩れるかのどちらかになる。

嘘の型（因果反転・人物すり替え・動機差し替え・時系列入れ替え・伏線型 など）の判定は**生成後に行う**。生成前に型を指定してモデルを誘導しない。判定結果はUI表示や口調の出し分けに使う。

---

## 技術スタック

| 層 | 選定 |
|---|---|
| フロント | Next.js (App Router) + TypeScript |
| チャットUI | Vercel AI SDK (`useChat`) |
| バックエンド | Next.js Route Handlers（別サーバーを立てない） |
| LLM | Gemini 2.5 Flash（`@ai-sdk/google` 経由） |
| カードKB | SQLite / libSQL (Turso)。ベクトル検索は `vector_top_k` |
| カード生成 | Python CLI（**アプリ外**、`tools/` 配下） |
| デプロイ | Vercel |

### 意図的に選んでいない技術

提案しないこと。理由があって外している。

- **Python バックエンドの分離** — 3日で結合を2回やる余裕がないため、TSに寄せている
- **Supabase / Postgres** — 単一ユーザー・カード数百枚・書き込みほぼ無しの要件に対して過剰。認証もストレージも使わない
- **LangChain 等のフレームワーク** — 処理が単純で、抽象層のデバッグコストの方が高い
- **ローカルLLM推論のアプリ組み込み** — オフライン前提は撤回済み

### LoRA について

口調の再現に LoRA を使うかは**未決**。まず Gemini にプロンプトのみで書かせて、「指摘されても訂正せず押し通す」態度が保てるか検証する。プロンプトで届くなら LoRA は入れない。届かない場合のみ、その差分を埋める目的で導入する。

勝手に学習パイプラインを組み始めないこと。

---

## ディレクトリ

```
app/
  api/chat/route.ts      チャット本体。retrieval → 生成 → 検証
  ...
lib/
  cards/                 カードの読み込み・検索
  verify/                検証層。生成結果とカードの照合
  prompt/                キャラクター定義・プロンプト組み立て
data/
  momotaro/cards.jsonl   ← コードはこの中身を知らない
  hxh/cards.jsonl
tools/
  generate_cards.py      あらすじテキスト → カードJSONL（Python CLI）
```

---

## 規約・注意点

### やらないこと

- **アプリに作品追加機能を付けない**。拡張性は「データを足せば動く」という構造で担保する。UIからの取り込みは作らない
- **作品名・キャラ名での条件分岐を書かない**。データとコードの分離が壊れる
- **`NEXT_PUBLIC_` に API キーを置かない**。LLM 呼び出しは必ず Route Handler 側
- **抽象化を先回りしない**。プラグイン機構のようなものは、2作品で実際に必要になるまで作らない

### カードを書くとき

- 既存作品のセリフ・地の文をそのまま写さない。事実を自分の言葉で1文にまとめる
- 1カード1主張。複数の事実を詰め込むと歪曲の取っ手にならない
- `salience` は知名度。高いカードほど嘘の土台として効く

### 環境変数

```
GEMINI_API_KEY=          # Google AI Studio で発行（GCPコンソールは不要）
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
```

カード生成 CLI 用のキーは**デモ用と分ける**。開発中の一括生成でレート制限を踏んでも、当日の枠を巻き込まないため。

Vercel 側の環境変数登録を忘れないこと（`.env.local` はデプロイに含まれない）。

---

## 開発の進め方

設計の議論は済んでいる。以降は**実装して触り、足りないところを埋める**進め方を取る。

ただし後から差しづらい2つは早めに着手する。

1. **カードの中身**。量が体験の質に直結し、しかも作業時間がかかる
2. **口調がプロンプトだけで保てるかの検証**。LoRA を入れるかの分岐になる